local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UIScroll = Instance.new("ScrollingFrame")
local UIListLayout = Instance.new("UIListLayout")

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Tạo khung chính chứa danh sách
MainFrame.Parent = ScreenGui
MainFrame.Size = UDim2.new(0, 250, 0, 350)
MainFrame.Position = UDim2.new(0.5, -125, 0.5, -175)
MainFrame.BackgroundColor3 = Color3.fromRGB(44, 62, 80)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true

-- Tạo danh sách cuộn
UIScroll.Parent = MainFrame
UIScroll.Size = UDim2.new(1, 0, 1, 0)
UIScroll.CanvasSize = UDim2.new(0, 0, 2, 0)
UIScroll.ScrollBarThickness = 8

UIListLayout.Parent = UIScroll
UIListLayout.Padding = UDim.new(0, 10)
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Hàm tạo nút cho từng kịch bản
local function createButton(text, callback)
    local button = Instance.new("TextButton")
    button.Parent = UIScroll
    button.Size = UDim2.new(1, -20, 0, 40)
    button.Text = text
    button.BackgroundColor3 = Color3.fromRGB(52, 152, 219)
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.Font = Enum.Font.GothamBold
    button.TextSize = 20
    button.BorderSizePixel = 0
    button.AutoButtonColor = true
    button.MouseButton1Click:Connect(callback)
end

-- Tạo các kịch bản có thể kích hoạt
createButton("Kịch bản 1: Kích hoạt A", function()
    getgenv().Key = "MARU-XL62E-C7MOC-3XU1-313GR-N4D1B"
    getgenv().id = "120797DFF1585661952"
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xshiba/MaruBitkub/main/Mobile.lua"))()
    print("Kịch bản 1 đã được kích hoạt")
end)

createButton("Kịch bản 2: Kích hoạt B", function()
    -- Thêm kịch bản khác tại đây
    print("Kịch bản 2 đã được kích hoạt")
end)

createButton("Kịch bản 3: Kích hoạt C", function()
    -- Thêm kịch bản khác tại đây
    print("Kịch bản 3 đã được kích hoạt")
end)

-- Tạo nút đóng
local closeButton = Instance.new("TextButton")
closeButton.Parent = MainFrame
closeButton.Size = UDim2.new(0, 250, 0, 40)
closeButton.Position = UDim2.new(0, 0, 1, -40)
closeButton.Text = "Đóng"
closeButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 20
closeButton.BorderSizePixel = 0
closeButton.AutoButtonColor = true
closeButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy() -- Đóng giao diện khi nhấn
end)
